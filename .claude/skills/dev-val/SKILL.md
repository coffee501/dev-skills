---
name: dev-val
description: Execute approved backend and integration tests, preserve auditable evidence, classify failures, govern retries and flaky tests, and evaluate validation gates. Use for verification, regression, retest, CI evidence review, and authorized non-functional experiments.
disable-model-invocation: true
---

# Claude Code Adapter

Before acting, read `${CLAUDE_SKILL_DIR}/../../../dev-val/SKILL.md` completely and follow it as the canonical workflow.
Treat `${CLAUDE_SKILL_DIR}/../../../dev-val` as the Skill root and resolve every relative reference from there.
This adapter changes discovery and invocation policy only; do not add, remove, or reinterpret core workflow rules.
